{
  "name": "mansara-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Mansara Foods B2B Distributor CRM, Inventory & Billing System (Standalone In-Browser Mock Edition)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
